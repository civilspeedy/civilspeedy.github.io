/**
 * Returns 'white' for true and 'black' for false
 * @param state Boolean value representing the state of the theme.
 * @returns 'white' or 'black'
 */
export function themeSwitch(state: boolean): string {
    return state ? 'white' : 'black';
}
